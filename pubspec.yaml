name: gestor_gado
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
  # --- PACOTE DE TRADUÇÃO (NECESSÁRIO PARA O CALENDÁRIO EM PORTUGUÊS) ---
  flutter_localizations:
    sdk: flutter
  # -----------------------------------------------------------------------

  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  provider: ^6.1.5+1
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  pdf: ^3.11.3
  printing: ^5.14.2
  csv: ^6.0.0
  intl: ^0.20.2
  share_plus: ^12.0.1
  fl_chart: ^1.1.1
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.2
  flutter_timezone: ^3.0.1
  file_picker: ^10.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true